# Glass

1.54" 240×240. Screen-on only for **showing** and for a **saved page**. The rest of the time it is off.

Jun is drawing the face by hand. Do not lock a character sheet. Do not drop stock wizard art in here.

Friend already blits:

- print-look stills (local SVG, 240×240) during `show` and on a kept page
- up to two short clips after the still (skipped if `FAL_KEY` is missing)
- then off

When face frames exist, they go here as blit assets. Friend should keep treating glass as a small page, not a UI toolkit.
